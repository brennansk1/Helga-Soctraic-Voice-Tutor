data/
*.log
__pycache__/
*.pyc
node_modules/
.env
backups/
*.db
*.db-shm
*.db-wal
.DS_Store
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage/
logs/
courses/
.claude/
_archive/Matterorn*
# Screenshots and scratch renders are ignored by default — they are usually
# debugging artefacts. The README's own images are the deliberate exception,
# so they must be listed here or `git add` silently does nothing.
*.png
!services/web-ui/static/img/*.svg
!docs/images/*.png
*.db
test_prompt_db
.venv*/

# Host-services virtualenv (built by scripts/host_services.sh instructions).
.venv-host/
