# Containerized fallback path (STT_BACKEND=faster-whisper, CPU).
# The primary nemotron-mlx backend runs NATIVELY on the Apple-Silicon host and
# is installed from requirements-host.txt — MLX/ANE cannot run in a Linux
# container.
flask==3.0.0

# faster-whisper is the PORTABLE FALLBACK, and it was commented out — so this
# image shipped with NO speech recognition of any kind and the service could
# only ever answer "backend unavailable". A fallback that is not installed is
# not a fallback. It is uncommented because a container whose entire purpose
# is the fallback path must contain the fallback.
faster-whisper==1.2.1
